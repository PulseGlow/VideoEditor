<Window x:Class="VideoEditor.Presentation.Views.ScreenRecorderAudioConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="配置屏幕录制音频"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        MinWidth="520"
        Background="{DynamicResource Brush.WindowBackground}"
        Foreground="{DynamicResource Brush.TextPrimary}">
    <Border Padding="18" 
            Background="{DynamicResource Brush.PanelBackground}" 
            BorderBrush="{DynamicResource Brush.PanelBorder}" 
            BorderThickness="1">
        <StackPanel>
            <TextBlock Text="请选择要录制的音频设备" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Margin="0,0,0,12"
                       Foreground="{DynamicResource Brush.TextPrimary}"/>
            <TextBlock Text="提示：将自动检测可用的 dshow 音频设备，您也可以手动输入设备名称。"
                       TextWrapping="Wrap" 
                       Opacity="0.8" 
                       Margin="0,0,0,12"
                       Foreground="{DynamicResource Brush.TextSecondary}"/>

            <StackPanel Margin="0,6,0,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="系统声音 (扬声器)：" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource Brush.TextPrimary}"/>
                    <Button Content="🔄 刷新设备列表" 
                            Margin="8,0,0,0" 
                            Padding="6,2" 
                            FontSize="11" 
                            Click="RefreshDevicesButton_Click"
                            Foreground="{DynamicResource Brush.TextPrimary}"
                            Style="{DynamicResource ButtonSecondaryStyle}"/>
                </StackPanel>
                <ComboBox x:Name="SystemAudioComboBox" 
                          Margin="0,4,0,0" 
                          Padding="6" 
                          IsEditable="True" 
                          DisplayMemberPath="DisplayName" 
                          SelectedValuePath="DeviceName"
                          Background="{DynamicResource Brush.ControlBackground}"
                          Foreground="{DynamicResource Brush.TextPrimary}"
                          BorderBrush="{DynamicResource Brush.ControlBorder}"
                          ToolTip="选择或输入系统音频设备名称"/>
                <TextBlock Text="留空则不录制系统输出；推荐安装 VB-Audio / virtual-audio-capturer 等虚拟声卡以捕获桌面声音。"
                           FontSize="11" 
                           Opacity="0.7" 
                           Margin="0,2,0,0"
                           Foreground="{DynamicResource Brush.TextSecondary}"/>
            </StackPanel>

            <StackPanel Margin="0,16,0,0">
                <TextBlock Text="麦克风/外接录音设备：" 
                           FontWeight="Bold" 
                           Margin="0,0,0,4"
                           Foreground="{DynamicResource Brush.TextPrimary}"/>
                <ComboBox x:Name="MicrophoneComboBox" 
                          Margin="0,4,0,0" 
                          Padding="6" 
                          IsEditable="True"
                          DisplayMemberPath="DisplayName" 
                          SelectedValuePath="DeviceName"
                          Background="{DynamicResource Brush.ControlBackground}"
                          Foreground="{DynamicResource Brush.TextPrimary}"
                          BorderBrush="{DynamicResource Brush.ControlBorder}"
                          ToolTip="选择或输入麦克风设备名称"/>
                <TextBlock Text="若无需录制麦克风可留空；若需要同时录制多支麦克风请使用混音软件。"
                           FontSize="11" 
                           Opacity="0.7" 
                           Margin="0,2,0,0"
                           Foreground="{DynamicResource Brush.TextSecondary}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="取消" 
                        Width="90" 
                        Margin="0,0,8,0" 
                        Click="CancelButton_Click"
                        Foreground="{DynamicResource Brush.TextPrimary}"
                        Style="{DynamicResource ButtonSecondaryStyle}"/>
                <Button Content="保存" 
                        Width="110" 
                        Click="SaveButton_Click"
                        Foreground="{DynamicResource Brush.TextPrimary}"
                        Style="{DynamicResource ButtonPrimaryStyle}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>

