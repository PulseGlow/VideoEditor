<Window x:Class="VideoEditor.Presentation.Views.FFmpegConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FFmpeg 配置"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        MinWidth="520"
        Background="{DynamicResource Brush.WindowBackground}"
        Foreground="{DynamicResource Brush.TextPrimary}">
    <Border Padding="18" 
            Background="{DynamicResource Brush.PanelBackground}" 
            BorderBrush="{DynamicResource Brush.PanelBorder}" 
            BorderThickness="1">
        <StackPanel>
            <TextBlock Text="FFmpeg 配置" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Margin="0,0,0,16"
                       Foreground="{DynamicResource Brush.TextPrimary}"/>

            <TextBlock Text="FFmpeg 可执行文件路径" 
                       FontWeight="Bold"
                       Foreground="{DynamicResource Brush.TextPrimary}"/>
            <DockPanel Margin="0,4,0,10">
                <TextBox x:Name="FFmpegPathTextBox" 
                         MinWidth="360" 
                         Padding="6"
                         Background="{DynamicResource Brush.ControlBackground}"
                         Foreground="{DynamicResource Brush.TextPrimary}"
                         BorderBrush="{DynamicResource Brush.ControlBorder}"/>
                <Button Content="浏览..." 
                        Width="80" 
                        Margin="6,0,0,0" 
                        Click="BrowseFFmpegButton_Click"
                        Style="{DynamicResource ButtonSecondaryStyle}"/>
            </DockPanel>

            <TextBlock Text="FFprobe 可执行文件路径（可选）" 
                       FontWeight="Bold"
                       Foreground="{DynamicResource Brush.TextPrimary}"/>
            <DockPanel Margin="0,4,0,10">
                <TextBox x:Name="FFprobePathTextBox" 
                         MinWidth="360" 
                         Padding="6"
                         Background="{DynamicResource Brush.ControlBackground}"
                         Foreground="{DynamicResource Brush.TextPrimary}"
                         BorderBrush="{DynamicResource Brush.ControlBorder}"/>
                <Button Content="浏览..." 
                        Width="80" 
                        Margin="6,0,0,0" 
                        Click="BrowseFFprobeButton_Click"
                        Style="{DynamicResource ButtonSecondaryStyle}"/>
            </DockPanel>

            <Button Content="🔍 自动搜索" 
                    Width="120" 
                    Margin="0,0,0,10"
                    Click="AutoSearchButton_Click"
                    Style="{DynamicResource ButtonPrimaryStyle}"
                    HorizontalAlignment="Left"/>

            <TextBlock Text="提示：程序会自动在 tools/ffmpeg 目录下搜索 FFmpeg 和 FFprobe。如果未找到，请手动指定路径。"
                       FontSize="11" 
                       Opacity="0.7" 
                       TextWrapping="Wrap" 
                       Margin="0,0,0,10"
                       Foreground="{DynamicResource Brush.TextSecondary}"/>

            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,12,0,0">
                <Button Content="取消" 
                        Width="90" 
                        Margin="0,0,8,0" 
                        Click="CancelButton_Click"
                        Style="{DynamicResource ButtonSecondaryStyle}"/>
                <Button Content="确定" 
                        Width="110" 
                        Click="OkButton_Click"
                        Style="{DynamicResource ButtonPrimaryStyle}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>

